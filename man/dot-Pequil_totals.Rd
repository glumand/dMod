% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{.Pequil_totals}
\alias{.Pequil_totals}
\title{Steady-state transformation with conserved moieties expressed as totals}
\usage{
.Pequil_totals(
  norm,
  ms,
  emit_d1,
  emit_d2,
  attach.input,
  keep.root,
  controlsODE,
  compile,
  modelname,
  condition,
  verbose,
  start.time,
  end.time,
  dotArgs
)
}
\arguments{
\item{norm}{Record from \link{.normalize_ss_inputs} with \code{fullsystem = TRUE}.}

\item{ms}{Resolved multistart controls.}

\item{emit_d1, emit_d2}{Whether first/second-order sensitivities are built.}

\item{attach.input, keep.root, controlsODE, compile, modelname, condition, verbose, start.time, end.time}{As in \link{Pequil}.}

\item{dotArgs}{Extra arguments forwarded to \link[CppODE:CppODE]{CppODE::CppODE}.}
}
\value{
A \link{parfn}.
}
\description{
Builds the \link{Pequil} parfn for the \code{expressInTotals = TRUE} case. The full
(uneliminated) system is integrated to its fixed point, so mass-action
positivity and the conservation laws hold automatically. Each integration
start is a random non-negative point on the conservation manifold fixed by
the totals (the mass is distributed across the moiety, not placed in a
single species), anchored at a private species and spread through the null
space of the CQ matrix. Total sensitivities are obtained from the pivot
species' initial-condition sensitivities via the constant map
\code{C[, pivots]^{-1}}, independent of the starting distribution.
}
\keyword{internal}
