% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlme.R
\name{modelNLME}
\alias{modelNLME}
\title{Adapter from a dMod prediction function to an \code{nlme}-compatible model}
\usage{
modelNLME(prdfn, covtable = NULL, cores = 1)
}
\arguments{
\item{prdfn}{A \code{prdfn} (e.g. \code{g * x * p}) describing the deterministic part of
the mixed-effects model.}

\item{covtable}{Optional \code{data.frame} whose column names label the subject
covariates. Used only to read the names — values are passed through \code{...} of
the returned closure.}

\item{cores}{Number of cores for \code{parallel::mclapply}. Forced to 1 on Windows.}
}
\value{
A function \code{model(time, name, ...)} that returns the prediction with a
\code{"gradient"} attribute.
}
\description{
Wraps a \link{prdfn} (typically \code{g * x * p}) so it can be passed to \code{nlme::nlme}.
The returned closure expects the per-observation arguments
\verb{time, name, <parameters>, <covariates>}, evaluates the prdfn for each unique
subject (defined by the covariate columns), and returns the residual prediction
with the gradient against parameters attached as \code{"gradient"}.
}
