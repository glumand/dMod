% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{.pimpl_solve_dfdx}
\alias{.pimpl_solve_dfdx}
\title{Solve \code{A \%*\% X = B} with SVD pseudoinverse fallback}
\usage{
.pimpl_solve_dfdx(A, B, row_names = rownames(A), warn_rcond = 1e-10)
}
\description{
Used by \link{Pimpl} to invert \code{df/dx} at the steady state. Well-conditioned
systems take a standard LU solve; ill-conditioned or rank-deficient
systems fall back to the Moore-Penrose pseudoinverse (minimum-norm
least-squares solution, equal to the IFT sensitivity restricted to the
constraint manifold), with a warning listing the null-space
direction(s) in \code{row_names} coordinates so the missing conserved
quantity or redundant equation can be identified.
}
\keyword{internal}
