% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlme.R
\name{msnlmeFit}
\alias{msnlmeFit}
\title{Multi-start nonlinear mixed-effects fit}
\usage{
msnlmeFit(
  obj,
  omega,
  center,
  prdfn = NULL,
  data = NULL,
  errfn = NULL,
  fixed = NULL,
  method = c("focei", "quadrature", "foceiQuadrature"),
  control = list(),
  fits = 20,
  cores = 1,
  samplefun = "rnorm",
  start1stfromCenter = FALSE,
  keepFull = FALSE,
  studyname = NULL,
  resultPath = ".",
  output = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{obj}{An \code{objfn} passed straight to \code{\link[=nlmeFit]{nlmeFit()}} (typically
\code{normL2(data, g*x*p) + constraintL2(mu = 0, Omega = om)}).}

\item{omega}{An \link{omega} spec with subject expansion.}

\item{center}{Named numeric or \link{parframe}. If numeric, the population
parameter vector around which random starts are sampled (structural pars
plus \code{omega$cholPars}). If a parframe, each row is used as a fixed
starting point and \code{fits} is overridden by \code{nrow(center)}.}

\item{prdfn}{The prediction function \code{g * x * p} used to build \code{obj}.}

\item{data}{The \link{datalist} used for \code{obj}.}

\item{errfn}{Optional obsfn defining a parameter-dependent error model.}

\item{fixed}{Optional named-numeric of fixed parameters.}

\item{method}{Estimator. Passed through to \code{\link[=nlmeFit]{nlmeFit()}}: \code{"focei"},
\code{"quadrature"}, or \code{"foceiQuadrature"}.}

\item{control}{Nested control list, passed through to \code{\link[=nlmeFit]{nlmeFit()}}.}

\item{fits}{Integer, number of random starts. Ignored when \code{center} is a
parframe.}

\item{cores}{Integer, number of parallel workers. On Unix uses
\code{parallel::mclapply} (fork); on Windows a PSOCK cluster + \code{foreach}.
Outer parallelism multiplies with the inner OpenMP threads of the C++
objective kernels (\code{getOption("dMod.objfn.threads")}); keep
\code{cores * dMod.objfn.threads} below your core count.}

\item{samplefun}{Name of a random-number generator (default \code{"rnorm"})
used to perturb \code{center}. Extra args in \code{...} whose names match
\code{formals(samplefun)} are forwarded.}

\item{start1stfromCenter}{Logical. If \code{TRUE}, the first fit starts at
\code{center} itself (no perturbation). Ignored when \code{center} is a parframe.}

\item{keepFull}{Logical. If \code{FALSE} (the default), each returned fit is
stripped of heavy state (\code{emDiag}, \code{prdfn}, \code{data}, \code{omega},
\code{errfn}, \code{foceiStart}, \code{stageTrace}) so the result stays small.
Set \code{TRUE} if you need to call \code{\link[=predict.nlmeFit]{predict.nlmeFit()}} / \code{\link[=plot.nlmeFit]{plot.nlmeFit()}}
etc. on individual fits.}

\item{studyname}{Optional character. If \code{output = TRUE}, fits are written
to \verb{<resultPath>/<studyname>/trial-N-<timestamp>/interRes/}. Defaults to
\code{"msnlmeFit"}.}

\item{resultPath}{Character, base directory for the on-disk dump.}

\item{output}{Logical. If \code{TRUE}, each fit is saved as it completes
(crash-resilient) and the full parlist is written at the end.}

\item{verbose}{Logical. If \code{TRUE}, prints per-fit progress and forwards
\code{verbose = TRUE} into \code{\link[=nlmeFit]{nlmeFit()}}.}

\item{...}{Forwarded to \code{samplefun} (e.g. \code{sd = 0.3}).}
}
\value{
A \link{parlist} of length \code{fits}, each element an \code{nlmeFit} (stripped
per \code{keepFull}) with \code{parinit} and \code{index} attached. Pass to
\code{\link[=as.parframe]{as.parframe()}} for a sorted-by-\code{value} table. Failed fits are stored
as \code{list(error = ..., value = NA, converged = FALSE, ...)}.
}
\description{
Runs \code{\link[=nlmeFit]{nlmeFit()}} from many starting points in parallel, returning a
\link{parlist} of fits sorted-ready for \code{\link[=as.parframe]{as.parframe()}} / \code{\link[=summary]{summary()}}. The
design mirrors \code{\link[=mstrust]{mstrust()}}: \code{center} is either a named-numeric (perturbed
by \code{samplefun} per fit) or a \link{parframe} (each row used as a starting
point). Use this to characterise the multi-modality of the marginal
likelihood and pick the best optimum.
}
\seealso{
\code{\link[=nlmeFit]{nlmeFit()}}, \code{\link[=mstrust]{mstrust()}}, \code{\link[=msParframe]{msParframe()}}, \link{parlist}.
}
