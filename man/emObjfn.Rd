% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlme.R
\name{emObjfn}
\alias{emObjfn}
\title{Build a quadrature-based NLME marginal-likelihood objective function}
\usage{
emObjfn(
  obj,
  omega,
  prdfn = NULL,
  data = NULL,
  errfn = NULL,
  method = "quadrature",
  control = list()
)
}
\arguments{
\item{obj}{An \code{objfn}, typically
\code{normL2(data, g*x*p, errmodel = err) + constraintL2(mu = 0, Omega = om)}.}

\item{omega}{An \link{omega} spec with subject expansion.}

\item{prdfn}{A \code{prdfn} (\code{g*x*p}). Required.}

\item{data}{A \link{datalist}. Required.}

\item{errfn}{Optional obsfn defining a parameter-dependent error model.}

\item{method}{Character. Only \code{"quadrature"} is supported. Retained for
forward compatibility; the historical \code{"laplace"} path now lives inside
the C++ FOCEI kernel reached via \link{nlmeFit} with \code{method = "focei"}.}

\item{control}{Named list with \code{level} (Smolyak depth, default 4) and
\code{cores} (default 1).}
}
\value{
A callable of class \code{c("emObjfn", "objfn", "fn")}. Calling it on
\code{pars} returns an \link{objlist} with an \code{emDiag} attribute carrying
quadrature diagnostics.
}
\description{
Returns a callable \code{em(pars)} that integrates out the per-subject random
effects using a sparse-grid Gauss-Hermite rule. The ECM E-step refreshes
the integration nodes between outer iterations via
\code{attr(em, "rebuildQuadrature")}. Most users want \link{nlmeFit}, which builds
the right \code{em} internally and runs a solver; use \code{emObjfn} directly only
if you need to evaluate the objective by hand.
}
\details{
The Laplace approximation that was historically available here as
\code{method = "laplace"} has been folded into the C++ FOCEI kernel exposed by
\link{nlmeFit} with \code{method = "focei"}. Call \code{nlmeFit} directly.
}
\seealso{
\link{nlmeFit}, \link{omega}
}
