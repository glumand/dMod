% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omegaClass.R
\name{updateOmegaChol}
\alias{updateOmegaChol}
\title{ECM closed-form update for the omegaSpec Cholesky parameters}
\usage{
updateOmegaChol(MHatList, omegaSpec)
}
\arguments{
\item{MHatList}{Length-N list of K x K positive-semidefinite matrices
(subject posterior second moments).}

\item{omegaSpec}{An \link{omega} spec object.}
}
\value{
Named numeric vector matching \code{omegaSpec$cholPars} (log-diagonal
on diagonals, free real on off-diagonals).
}
\description{
Given a list of per-subject posterior second moments
\code{MHat_i = Ehat[eta_i \%*\% t(eta_i) | y_i]} (the output of the ECM E-step
implemented in \link{ecmEvaluateSubject}), returns the cholPars vector that
maximises the EM Q-function
\deqn{Q(\Omega) = -\frac{1}{2} N \, (\log|\Omega| + \mathrm{tr}(\Omega^{-1} S)),
  \quad S = \frac{1}{N}\sum_i \hat M_i,}
subject to the sparsity / log-Cholesky parametrisation encoded in
\code{omegaSpec}. For pure-diagonal and full structures the solution is closed
form; for selective \code{correlate} it is the minimiser of an analytic gradient
obtained via \link{trust} with finite-difference Hessian (convex small-dim
problem, converges in a handful of iterations).
}
\seealso{
\link{omega}, \link{ecmEvaluateSubject}
}
