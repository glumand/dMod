% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{mcmc}
\alias{mcmc}
\title{Posterior sampling for dMod objective functions}
\usage{
mcmc(
  target,
  sequenceType = c("single", "sequential"),
  sequenceSchedule = c("adaptiveEss", "fixed", "learned"),
  populationSize = 1L,
  reSampling = c("systematic", "stratified", "residual", "multinomial", "none"),
  moveType = c("langevin", "mh", "hmc", "nuts", "pathIntegral"),
  metric = c("euclidean", "riemannFisher", "riemannHessian", "fixed"),
  parinit = NULL,
  nIter = 1000L,
  warmup = NULL,
  chains = 1L,
  cores = 1L,
  chainCores = NULL,
  seeds = NULL,
  parlower = NULL,
  parupper = NULL,
  parscale = NULL,
  sequenceControl = NULL,
  moveControl = NULL,
  metricControl = NULL,
  retry = TRUE,
  nTries = 10L,
  ...
)
}
\arguments{
\item{target}{Either an \code{objfn}, a \code{\link[=flatTarget]{flatTarget()}}, or one of the
NLME target constructors. An \code{objfn} is silently wrapped to a flat
target (no prior); pass an explicit \code{\link[=flatTarget]{flatTarget()}} when you want to
supply \code{priorObj} / \code{priorSample} for sequential SMC.}

\item{sequenceType}{\code{"single"} (one or more independent chains, each
moving with the chosen \code{moveType}) or \code{"sequential"} (SMC tempering
along an annealed-likelihood path, requires \code{priorSample} in the
target).}

\item{sequenceSchedule}{For sequential sampling: \code{"fixed"} (uses
\code{smcControl()$schedule}), \code{"adaptiveEss"} (ESS-bisection;
recommended default), or \code{"learned"} (slot only; not implemented).}

\item{populationSize}{Number of particles for SMC.}

\item{reSampling}{Per-level particle resampling scheme.}

\item{moveType}{Inner move kernel: \code{"mh"} / \code{"langevin"} / \code{"hmc"} /
\code{"nuts"} are real implementations; \code{"pathIntegral"} is a slot.}

\item{metric}{Metric tensor for \code{langevin} / \code{hmc} / \code{nuts}:
\code{"euclidean"} (G = I), \code{"riemannFisher"} (G = local Fisher info),
\code{"riemannHessian"} (placeholder, falls back to Fisher), \code{"fixed"}
(user-supplied via the move control).}

\item{parinit}{Optional named numeric. Required for \code{"single"} mode;
unused in \code{"sequential"} (initial particles come from \code{priorSample}).}

\item{nIter}{Post-warmup samples per chain (single mode) or per
particle (joint Particle-Gibbs).}

\item{warmup}{Warmup iterations (single mode) or burn-in sweeps
(joint Particle-Gibbs). Ignored for \code{"sequential"} (tempering plays
the role of warmup); a warning is emitted if non-zero.}

\item{chains}{Number of independent chains. Setting \code{chains > 1}
wraps the sampler with multi-chain orchestration and computes
Gelman-Rubin R-hat.}

\item{cores}{Parallel workers for particle / chain moves.}

\item{chainCores}{Outer-axis cores when \code{chains > 1}. Defaults to
\code{min(chains, parallel::detectCores())}.}

\item{seeds}{Optional integer vector of length \code{chains} to seed each
chain. Defaults to \code{seq_len(chains)}.}

\item{parlower, parupper, parscale}{Forwarded to the inner kernel.
Bounds are enforced by immediate rejection of out-of-box proposals.}

\item{sequenceControl, moveControl, metricControl}{Typed control objects
from the per-axis constructors. Sensible defaults are used when the
user passes \code{NULL} (which is the default).}

\item{retry}{Logical, only honoured for \code{sequenceType = "sequential"}
(SMC). If \code{TRUE} (default), a particle whose initial likelihood
evaluation fails (NULL or non-finite) is redrawn from \code{priorSample}
up to \code{nTries} times before being flagged with \code{-Inf}. Single-chain
MCMC does not retry: failed proposals are rejected by the
Metropolis kernel, which is the detailed-balance-correct behaviour.}

\item{nTries}{Maximum number of prior draws per particle slot at SMC
initialisation, including the first. Default \code{10L}.}

\item{...}{Additional arguments forwarded as dots into the objfn.}
}
\value{
An object of class \code{c("mcmcResult", ...)}, subclassed to
\code{mcmcResultSingle}, \code{mcmcResultSequential} (carries \code{logEvidence},
\code{betaPath}, \code{ESSPath}, \code{acceptRates}, \code{stepsizePath}, \code{nLevels}),
\code{mcmcResultBlocked} (adds \code{etaSamples}, \code{acceptOuter}, \code{acceptEta}),
or \code{mcmcResultMulti} (adds \code{chainId}, \code{rHat}) depending on the run
configuration.
}
\description{
MCMC sampler with single-chain (\code{sequenceType = "single"}) and SMC
(\code{sequenceType = "sequential"}) modes. A bare \code{objfn} passed as \code{target}
is auto-wrapped to a \code{\link[=flatTarget]{flatTarget()}}; hierarchical NLME posteriors are
built via \code{\link[=bayesNLMEMarginal]{bayesNLMEMarginal()}} or \code{\link[=bayesNLMEJoint]{bayesNLMEJoint()}}.
}
\seealso{
\code{\link[=flatTarget]{flatTarget()}}, \code{\link[=bayesNLMEMarginal]{bayesNLMEMarginal()}}, \code{\link[=bayesNLMEJoint]{bayesNLMEJoint()}},
\link{mcmcControl}, \code{\link[=priorOmega]{priorOmega()}}, \code{\link[=metricContext]{metricContext()}}
}
