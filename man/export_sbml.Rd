% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SBMLinterface.R
\name{export_sbml}
\alias{export_sbml}
\title{Export an eqnlist to an SBML Level 3 file}
\usage{
export_sbml(
  eqnlist,
  parameters = NULL,
  inits = NULL,
  filepath,
  model_id = "dMod_export",
  amicipath = NULL
)
}
\arguments{
\item{eqnlist}{Object of class \link{eqnlist} to export.}

\item{parameters}{Named numeric vector of parameter values (including any
compartment-size parameters referenced in \code{eqnlist$compartments}). Pass
\code{NULL} to write parameters without values.}

\item{inits}{Named numeric \emph{or} character vector of initial values keyed
by state name. Numeric entries (or character entries that parse as
numeric) are written as \code{initialConcentration}; non-numeric character
entries are emitted as \verb{<initialAssignment>} formulas and let the SBML
simulator resolve the expression against \code{parameters} at sim time.
Missing states default to 0.}

\item{filepath}{Path to the SBML output file.}

\item{model_id}{SBML model identifier. Defaults to \code{"dMod_export"}.}

\item{amicipath}{Optional \code{PYTHONPATH} entry prepended to the python call,
matching the knob on \code{\link[=import_sbml]{import_sbml()}}.}
}
\value{
\code{filepath}, invisibly.
}
\description{
Serialises an \link{eqnlist} plus parameter values and initial concentrations to
SBML Level 3 Version 2. Each reaction's kinetic law is emitted as
\code{V_home * rate_dMod} to restore SBML's extensive-flux convention
(\code{K_SBML = rate_dMod * V}). Requires a Python environment with \code{libsbml}
installed; the default location matches the one used by \code{\link[=import_sbml]{import_sbml()}}.
}
