% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlme.R
\name{ecmEvaluateSubject}
\alias{ecmEvaluateSubject}
\title{Per-subject adaptive-quadrature marginal-likelihood evaluator}
\usage{
ecmEvaluateSubject(
  subjIdx,
  psiFull,
  etaModes,
  omega,
  nodesSubj,
  xPred,
  datalist,
  errfn = NULL,
  fixed = NULL,
  outerActiveNames = NULL,
  mode = c("moments_only", "with_grad")
)
}
\arguments{
\item{subjIdx}{Integer in \verb{1..N} selecting the subject.}

\item{psiFull}{Named numeric, the full outer parameter vector at which to
evaluate (structural + chol_pars; chol_pars are frozen during CM-1).}

\item{etaModes}{N x K matrix of all subjects' eta values; the row at
\code{subjIdx} is ignored. Other rows are passed through to the prediction
call (required for parameter completeness in joint models).}

\item{omega}{An \link{omega} spec object with subject expansion.}

\item{nodesSubj}{Output of \code{makeSubjectNodes(eta_hat_i, H_i, level)} for
the active subject.}

\item{xPred}{The prediction function (e.g. \code{g * x * p}).}

\item{datalist}{The \link{datalist} used for the joint objective.}

\item{errfn}{Optional obsfn (passed through to
\link{evalConditionResidual}).}

\item{fixed}{Optional fixed-parameter vector.}

\item{outerActiveNames}{Character vector of parameter names to track
gradient/Hessian for. Defaults to \code{names(psiFull)}. For CM-1 with frozen
chol_pars, pass just the structural names.}

\item{mode}{\code{"moments_only"} (no gradient/Hessian, cheap) or
\code{"with_grad"} (gradient + Hessian of \verb{-2 log L_i}).}
}
\value{
A list with components:
\describe{
\item{\code{logLhat}}{\verb{log L_i} (scalar).}
\item{\code{m_hat}}{Length-K named numeric, posterior mean of \eqn{\eta_i}.}
\item{\code{M_hat}}{K x K named matrix, posterior 2nd moment
\eqn{\hat E[\eta_i \eta_i^T \mid y_i]}.}
\item{\code{maxSoftmax}}{Max |softmax weight| across nodes; diagnostic of
grid concentration.}
\item{\code{n_eff}}{Effective node count, 1/sum(softmax^2); diagnostic.}
\item{\code{gradient}, \code{hessian}}{When \code{mode = "with_grad"}: gradient and
Hessian of \verb{-2 log L_i} w.r.t. \code{outerActiveNames}. NULL otherwise.}
}
}
\description{
Evaluates the per-subject marginal likelihood
\eqn{\hat L_i = \int p(y_i \mid \eta) \, N(\eta \mid 0, \Omega)\, d\eta}
via sparse-grid Gauss-Hermite quadrature at a precomputed set of nodes
(output of \link{makeSubjectNodes}). Returns the log-likelihood, posterior
first and second moments of \eqn{\eta_i}, plus (optionally) the gradient
and Hessian of \eqn{-2 \log \hat L_i} with respect to a chosen subset of
outer (population) parameters.

This helper bypasses \link{normL2} / \link{constraintL2} entirely: the data
likelihood contribution comes from the lifted \link{evalConditionResidual}
(one prediction call per node, single-condition), the MVN prior on the
subject's \eqn{\eta_b} is added in closed form via \code{omega$buildL}.
Avoids the multi-condition parameter rebinding and full-population MVN
contribution that calling \code{obj(..., conditions = subjects[i])} per node
would incur.
}
\seealso{
\link{makeSubjectNodes}, \link{evalConditionResidual}
}
\keyword{internal}
