% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmcClass.R
\name{flatTarget}
\alias{flatTarget}
\title{Construct a flat MCMC target}
\usage{
flatTarget(likObj, priorObj = NULL, priorSample = NULL)
}
\arguments{
\item{likObj}{An \code{objfn} returning \eqn{-2 \log p(y \mid \theta)},
typically from \code{\link[=normL2]{normL2()}}.}

\item{priorObj}{Optional \code{objfn} returning \eqn{-2 \log p(\theta)}.
May be \code{NULL} (improper flat prior).}

\item{priorSample}{Optional function \verb{function(n)} returning an
\verb{n x K} matrix of prior draws with column names matching the
likelihood's parameter names. Required when \code{\link[=mcmc]{mcmc()}} is called
with \code{sequenceType = "sequential"}.}
}
\value{
An object of class \code{c("mcmcTarget", "flatTarget", "list")}.
}
\description{
Bundles a likelihood objfn, an optional prior objfn, and an optional
prior-sample generator into a single target object consumable by
\code{\link[=mcmc]{mcmc()}}. Both objfns must follow dMod's \verb{-2 log p} convention; the
kernel converts to log-posterior space internally.
}
\seealso{
\code{\link[=mcmc]{mcmc()}}, \code{\link[=bayesNLMEMarginal]{bayesNLMEMarginal()}}, \code{\link[=bayesNLMEJoint]{bayesNLMEJoint()}}
}
