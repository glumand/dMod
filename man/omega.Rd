% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foceiClass.R
\name{omega}
\alias{omega}
\title{Specify random-effect covariance structure for FOCEI}
\usage{
omega(
  eta,
  structure = c("diag", "full"),
  correlate = NULL,
  subjects = NULL,
  prefix = "omega"
)
}
\arguments{
\item{eta}{Character vector of base eta names, one per random effect, e.g.
\code{c("eta_Cl", "eta_V", "eta_Ka")}. Determines the dimension \code{K}.}

\item{structure}{One of \code{"diag"} (default, only diagonal Cholesky entries
estimated, K free parameters) or \code{"full"} (full lower-triangular Cholesky,
K(K+1)/2 free parameters).}

\item{correlate}{Optional list of length-2 character vectors selecting
off-diagonal Cholesky entries to be estimated. Alternative to
\code{structure = "full"}. Each entry is a pair of eta names whose correlation
is freed.}

\item{subjects}{Optional character vector of subject identifiers. When given,
the per-subject random-effect parameter names are constructed as
\code{paste0(eta[k], "_", subjects[i])} and stored in \code{subject_etas}. Required
downstream by \link{focei} but optional here.}

\item{prefix}{Character string used as a name stem for Cholesky parameters.
Default \code{"omega"}. With \code{eta = c("eta_Cl", "eta_V")} this yields
\code{omega_Cl_Cl}, \code{omega_V_V}, \code{omega_V_Cl}.}
}
\value{
An \code{omegaSpec} (S3 list) with components:
\describe{
\item{\code{K}}{Dimension.}
\item{\code{eta}}{Base eta names.}
\item{\code{chol_pars}}{Character vector of Cholesky parameter names in
column-major order.}
\item{\code{name_matrix}}{\verb{K x K} character matrix; entry \verb{[k, l]} is the
parameter name of \code{L[k, l]} or \code{NA} if structurally zero.}
\item{\code{is_diag}}{Named logical vector: TRUE for log-diagonal Cholesky
parameters, FALSE for free off-diagonal entries.}
\item{\code{chol_loc}}{\verb{length(chol_pars) x 2} integer matrix giving the
\verb{(row, col)} location of each Cholesky parameter in \code{L}.}
\item{\code{subject_etas}}{\verb{N x K} character matrix of per-subject eta names,
or \code{NULL} if \code{subjects} was not supplied.}
\item{\code{build_L(chol_vec)}}{Function returning the lower-triangular \code{L}
given a named numeric vector containing at least \code{chol_pars}.}
}
}
\description{
Builds an \code{omegaSpec} object describing a Cholesky-parametrised random-effect
covariance matrix \verb{Omega = L L^T}, with \code{L} lower-triangular, log-diagonal,
and arbitrary sparsity below the diagonal. The result is consumed by
\link{constraintL2} (when its \code{Omega} argument is set) and by \link{focei}.
}
\details{
The diagonal of \code{L} is parametrised on the log scale to enforce positive
definiteness of \code{Omega}. Off-diagonal entries are unrestricted real numbers.
This is the standard pharmacometric choice and matches \code{lme4}/\code{TMB}
conventions.
}
\examples{
om1 <- omega(eta = c("eta_Cl", "eta_V"))
om1$chol_pars
om1$build_L(c(omega_Cl_Cl = log(0.3), omega_V_V = log(0.2)))

om2 <- omega(eta = c("eta_Cl", "eta_V", "eta_Ka"), structure = "full",
             subjects = paste0("subj", 1:3))
om2$subject_etas
om2$chol_pars

}
\seealso{
\link{constraintL2}, \link{focei}
}
