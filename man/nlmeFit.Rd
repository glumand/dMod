% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlme.R
\name{nlmeFit}
\alias{nlmeFit}
\title{Fit a nonlinear mixed-effects model}
\usage{
nlmeFit(
  joint,
  omegaSpec,
  init,
  model = NULL,
  data = NULL,
  errmodel = NULL,
  fixed = NULL,
  method = c("focei", "quadrature", "foceiQuadrature"),
  control = list(),
  verbose = TRUE
)
}
\arguments{
\item{joint}{An \code{objfn} of the form
\code{normL2(data, model, errmodel = err) + constraintL2(mu = 0, Omega = om)}.}

\item{omegaSpec}{An \link{omega} spec with subject expansion.}

\item{init}{Named numeric starting parameter vector. Must contain all
structural parameters and all \code{omegaSpec$cholPars}.}

\item{model}{The prediction function \code{g * x * p} used to build \code{joint}.
Required.}

\item{data}{The \link{datalist} used for \code{joint}. Required.}

\item{errmodel}{Optional obsfn defining a parameter-dependent error model.}

\item{fixed}{Optional named-numeric of fixed parameters.}

\item{method}{Estimator. \code{"focei"} runs the C++ FOCEI kernel
(Laplace + trust + eager Stage-2 correction); \code{"quadrature"} runs
adaptive sparse-grid Gauss-Hermite + ECM with a cold start;
\code{"foceiQuadrature"} runs FOCEI first and uses the converged
structural pars and modes as warmstart for the quadrature polish.}

\item{control}{Nested list of method-specific knobs. Entries:
\describe{
\item{\verb{$focei}}{Recognised keys: \code{innerControl}, \code{trustControl}.}
\item{\verb{$quadrature}}{Passed to the quadrature \link{emObjfn} and ECM solver.
Recognised keys: \code{level}, \code{cores}, \code{epsQuadLevels}, \code{epsEcm},
\code{epsOfvRel}, \code{maxEcmPerStage}, \code{maxCm1Iter}, \code{cm1Control}.}
}}

\item{verbose}{Logical. If TRUE prints solver progress.}
}
\value{
An \code{nlmeFit} S3 list with fields \code{argument}, \code{value}, \code{gradient},
\code{hessian}, \code{omega}, \code{etaModes}, \code{converged}, \code{iterations}, \code{emDiag},
\code{method}, \code{foceiStart}, \code{stageTrace}, \code{model}, \code{data}, \code{omegaSpec},
\code{errmodel}.
}
\description{
Builds the marginal-likelihood objective via \link{emObjfn} and runs the
selected estimator. Returns an \code{nlmeFit} S3 object consumable by the
diagnostic helpers (\link{predict.nlmeFit}, \link{plot.nlmeFit}, \link{plotIndivs} etc.).
}
\seealso{
\link{emObjfn}, \link{omega}, \link{predict.nlmeFit}
}
