% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmcClass.R
\name{bayesNLMEMarginal}
\alias{bayesNLMEMarginal}
\title{Bayesian NLME target with Laplace-marginalised etas}
\usage{
bayesNLMEMarginal(
  obj,
  omegaSpec,
  prdfn,
  data,
  init = NULL,
  priorTheta = NULL,
  priorOmegaObj = NULL,
  priorSample = NULL,
  errfn = NULL,
  control = list()
)
}
\arguments{
\item{obj}{A joint \code{objfn}, conventionally
\code{normL2(data, prdfn) + constraintL2(mu = 0, Omega = omegaSpec)}.}

\item{omegaSpec}{An \code{omegaSpec} from \code{\link[=omega]{omega()}} with subject expansion.}

\item{prdfn}{The prediction function used inside \code{obj}.}

\item{data}{The \link{datalist} used inside \code{obj}.}

\item{init}{Optional named numeric naming the outer parameters.
Must contain structural names plus all of \code{omegaSpec$cholPars}.
When \code{NULL}, structural names are inferred from
\code{attr(obj, "parameters")} minus subject etas and cholPars.}

\item{priorTheta}{Optional \code{objfn} returning
\eqn{-2 \log p(\theta_{\text{struct}})}.}

\item{priorOmegaObj}{Optional \code{objfn} from \code{\link[=priorOmega]{priorOmega()}} over
\code{omegaSpec$cholPars}.}

\item{priorSample}{Function \verb{function(n)} returning an \verb{n x P} matrix
of prior draws with column names matching the structural and
Cholesky parameter names. Required for sequential SMC sampling.}

\item{errfn}{Optional error model (\code{obsfn}).}

\item{control}{Named list with optional \code{innerControl} and
\code{trustControl} lists forwarded to focei_inner_trust.}
}
\value{
An object of class
\code{c("mcmcTarget", "bayesNLMEMarginal", "flatTarget", "list")},
consumed by \code{\link[=mcmc]{mcmc()}}.
}
\description{
MCMC target for the posterior
\eqn{p(\theta_{\text{struct}}, \omega_{\text{chol}} \mid y)} with the
per-subject \eqn{\eta_i} integrated out via FOCEI's Laplace
approximation. The marginal likelihood matches the frequentist
\code{\link[=nlmeFit]{nlmeFit()}} path with \code{method = "focei"}.
}
\seealso{
\code{\link[=mcmc]{mcmc()}}, \code{\link[=bayesNLMEJoint]{bayesNLMEJoint()}}, \code{\link[=priorOmega]{priorOmega()}}, \code{\link[=omega]{omega()}}
}
