% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{mcmcControl}
\alias{mcmcControl}
\alias{mhControl}
\alias{langevinControl}
\alias{hmcControl}
\alias{nutsControl}
\alias{pathIntegralControl}
\alias{smcControl}
\alias{metricControl}
\title{Control constructors for \code{\link[=mcmc]{mcmc()}}}
\usage{
mhControl(
  stepsize = NULL,
  acceptTarget = 0.234,
  proposalCov = c("identity", "fisher", "fixed"),
  covFixed = NULL,
  ridge = 1e-08,
  daGamma = 0.05,
  daT0 = 10,
  daKappa = 0.75
)

langevinControl(
  stepsize = NULL,
  acceptTarget = 0.574,
  preconditioner = c("local", "fixed", "identity"),
  correction = c("none", "full"),
  GFixed = NULL,
  ridge = 1e-08,
  daGamma = 0.05,
  daT0 = 10,
  daKappa = 0.75
)

hmcControl(
  stepsize = NULL,
  leapfrogSteps = 10L,
  massMatrix = c("identity", "fisher", "fixed"),
  MFixed = NULL,
  acceptTarget = 0.8,
  ridge = 1e-08,
  daGamma = 0.05,
  daT0 = 10,
  daKappa = 0.75
)

nutsControl(
  stepsize = NULL,
  maxTreeDepth = 10L,
  deltaMax = 1000,
  massMatrix = c("identity", "fisher", "fixed"),
  MFixed = NULL,
  acceptTarget = 0.8,
  ridge = 1e-08,
  daGamma = 0.05,
  daT0 = 10,
  daKappa = 0.75
)

pathIntegralControl(P = 8L, hbar = 1)

smcControl(
  nParticles = 1000L,
  essThreshold = 0.5,
  malaSteps = 5L,
  schedule = list(),
  parallelStrategy = c("forks", "serial"),
  continuousAdaption = TRUE,
  verbose = FALSE,
  blather = FALSE,
  stepsize = NULL
)

metricControl(
  metricContext = NULL,
  ridge = 1e-08,
  fisherMode = c("gn", "exact")
)
}
\arguments{
\item{stepsize}{Initial step size. \code{NULL} triggers a per-move heuristic
refined by dual averaging during warmup.}

\item{acceptTarget}{Target Metropolis acceptance probability for dual
averaging adaption.}

\item{proposalCov}{For \code{\link[=mhControl]{mhControl()}}: one of \code{"identity"}, \code{"fisher"}
(use Fisher information from the first objfn call), or \code{"fixed"}
(use \code{covFixed}).}

\item{covFixed}{Fixed K x K proposal covariance for \code{proposalCov = "fixed"}.}

\item{ridge}{Adaptive-ridge floor for the metric Cholesky.}

\item{daGamma, daT0, daKappa}{Dual-averaging hyperparameters
(Hoffman-Gelman 2014).}

\item{preconditioner}{For \code{\link[=langevinControl]{langevinControl()}}: one of \code{"local"}
(Fisher recomputed each step), \code{"fixed"} (use \code{GFixed}), or \code{"identity"}.}

\item{correction}{For \code{\link[=langevinControl]{langevinControl()}}: \code{"none"} (simplified mMALA)
or \code{"full"} (RMALA with Christoffel drift; requires \code{metricContext}).}

\item{GFixed}{K x K Fisher information used when
\code{preconditioner = "fixed"}. Expected in \verb{-2 log L} units (matches
\code{trust()$hessian}); the kernel divides by 2 internally.}

\item{leapfrogSteps}{Number of leapfrog steps per HMC iteration.}

\item{massMatrix}{For \code{\link[=hmcControl]{hmcControl()}} / \code{\link[=nutsControl]{nutsControl()}}: \code{"identity"},
\code{"fisher"} (frozen at parinit), or \code{"fixed"} (use \code{MFixed}).}

\item{MFixed}{Fixed K x K mass matrix for \code{massMatrix = "fixed"}.}

\item{maxTreeDepth}{Maximum NUTS tree depth.}

\item{deltaMax}{NUTS divergence threshold on the Hamiltonian.}

\item{P}{Path-integral replica count (slot only; not yet implemented).}

\item{hbar}{Path-integral coupling (slot only; not yet implemented).}

\item{nParticles}{Number of SMC particles.}

\item{essThreshold}{ESS-fraction trigger for the adaptive-beta bisection.}

\item{malaSteps}{Inner move iterations per SMC level.}

\item{schedule}{Optional fixed beta ladder for
\code{sequenceSchedule = "fixed"}. Numeric vector starting at 0, ending at 1.}

\item{parallelStrategy}{Particle-parallelism: \code{"forks"} (mclapply on Unix,
serial on Windows) or \code{"serial"}.}

\item{continuousAdaption}{If \code{TRUE}, dual-averaging state persists across
SMC levels rather than resetting per level.}

\item{verbose}{Print one line per SMC level.}

\item{blather}{Attach full per-level particle / weight history.}

\item{metricContext}{A \code{\link[=metricContext]{metricContext()}} object, required when
\code{correction = "full"} in \code{\link[=langevinControl]{langevinControl()}}.}

\item{fisherMode}{Reserved for future Fisher-vs-exact-Hessian selection.}
}
\value{
A tagged control list consumed by \code{\link[=mcmc]{mcmc()}}.
}
\description{
Typed parameter bundles for each axis of the \code{\link[=mcmc]{mcmc()}} sampler.
\code{mhControl} / \code{langevinControl} / \code{hmcControl} / \code{nutsControl} /
\code{pathIntegralControl} configure the inner move kernel; \code{smcControl}
configures the SMC tempering loop; \code{metricControl} configures
geometry options shared by Langevin / HMC / NUTS.
}
