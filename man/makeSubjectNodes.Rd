% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadrature.R
\name{makeSubjectNodes}
\alias{makeSubjectNodes}
\title{Build per-subject quadrature nodes for adaptive NLME marginal integration}
\usage{
makeSubjectNodes(etaHat, Hi, level)
}
\arguments{
\item{etaHat}{Length-K numeric, posterior mode of the random effects.}

\item{Hi}{K x K positive-definite matrix, the joint's negative-Hessian
at \code{etaHat} (= inverse covariance of the local Gaussian).}

\item{level}{Integer >= K, Smolyak depth. K+1 to K+3 is the useful range
for typical NLME problems; higher levels exponentially raise node count.}
}
\value{
A list with components:
\describe{
\item{\code{etaNodes}}{B x K matrix of quadrature nodes in eta-space
(batch-first; row \code{b} is the b-th node).}
\item{\code{logAbsWeights}}{Length-B numeric, \verb{log|W_b|} of the augmented
weights including the change-of-variable Jacobian. Use with
\code{weightSigns} for downstream signed log-sum-exp.}
\item{\code{weightSigns}}{Length-B numeric \verb{\{-1, +1\}}, sign of \code{W_b}.}
\item{\code{K}, \code{level}}{Echoed.}
}
}
\description{
Given the posterior mode \code{etaHat} and negative-Hessian \code{Hi} of the joint
log-density at the mode (= inverse covariance of the local Gaussian
approximation), constructs sparse-grid Gauss-Hermite quadrature nodes in
eta-space along with the log-magnitudes of their augmented weights (with
signs tracked separately).

The augmented weight \code{W_b} for the integral \verb{int g(eta) deta} approximated
as \verb{sum_b W_b * g(eta_b)} includes: the raw Smolyak-Gauss-Hermite weight
\code{w_b_GH}, the change-of-variable Jacobian \verb{2^(K/2) / |det L_H|}, and the
\verb{exp(z_b' z_b)} factor that un-does the implicit \verb{exp(-z'z)} weight of the
physicists' GH rule. Combined: \verb{log W_b = (K/2)*log(2) - log|det L_H| + log|w_b_GH| + z_b' z_b}, with sign carried in \code{weightSigns}.
}
\seealso{
\code{sparse_grid_gh} (Rcpp-exported, in \code{src/quadrature.cpp}).
}
