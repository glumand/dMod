% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmcClass.R
\name{priorOmega}
\alias{priorOmega}
\title{Hyperprior on a random-effects covariance Omega}
\usage{
priorOmega(
  omegaSpec,
  kind = c("LKJHalfNormal", "LKJHalfCauchy", "inverseWishart"),
  lkjEta = 2,
  scaleSD = 1,
  df = NULL
)
}
\arguments{
\item{omegaSpec}{An \code{omegaSpec} from \code{\link[=omega]{omega()}}.}

\item{kind}{Prior family. \code{"LKJHalfNormal"} and \code{"LKJHalfCauchy"} use the
indicated marginal-scale prior together with LKJ on the correlation.
\code{"inverseWishart"} is not yet implemented.}

\item{lkjEta}{LKJ shape \eqn{\eta > 0}. Default 2 (mildly favours the
identity correlation). Ignored when \code{omegaSpec} is purely diagonal.}

\item{scaleSD}{Scale of the marginal-SD prior. Default 1.}

\item{df}{Degrees of freedom for the unimplemented \code{inverseWishart}
variant.}
}
\value{
An \code{objfn} returning an \link{objlist} with \verb{value = -2 log p},
\code{gradient} and \code{hessian} over \code{omegaSpec$cholPars}.
}
\description{
Returns an \code{objfn} over \code{omegaSpec$cholPars} representing
\eqn{-2 \log p(\Omega)}: LKJ(\eqn{\eta}) on the correlation matrix
combined with a half-Normal or half-Cauchy prior on the marginal
standard deviations \eqn{\sigma_k = \sqrt{\Omega_{kk}}}. Gradient and
Hessian are evaluated analytically (the Hessian is block-diagonal by row
of \eqn{L}).
}
\references{
Lewandowski, D., Kurowicka, D. and Joe, H. (2009). Generating random
correlation matrices based on vines and extended onion method. J. Mult.
Anal. 100(9), 1989-2001.

Gelman, A. (2006). Prior distributions for variance parameters in
hierarchical models. Bayesian Analysis 1(3), 515-534.
}
\seealso{
\code{\link[=omega]{omega()}}, \code{\link[=constraintL2]{constraintL2()}}, \code{\link[=mcmc]{mcmc()}}
}
