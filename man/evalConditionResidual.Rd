% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objClass.R
\name{evalConditionResidual}
\alias{evalConditionResidual}
\title{Per-condition residual contribution to an L2 objective}
\usage{
evalConditionResidual(
  dataI,
  predictionI,
  pars,
  errmodel = NULL,
  fixed = NULL,
  cn = NULL,
  eCondNames = NULL,
  bessel = 1,
  deriv = TRUE,
  deriv2 = FALSE
)
}
\arguments{
\item{dataI}{datalist entry for one condition (data.frame with
\code{name}, \code{time}, \code{value}, \code{sigma} columns).}

\item{predictionI}{prdframe for that condition (typically \code{prediction[[cn]]}
from a prdfn call).}

\item{pars}{Named numeric parameter vector at which to evaluate.}

\item{errmodel}{Optional obsfn defining a parameter-dependent error model.}

\item{fixed}{Optional fixed-parameter vector (passed through to errmodel).}

\item{cn}{Character condition name. Required when \code{errmodel} is set, used
for errmodel condition routing.}

\item{eCondNames}{Optional character vector of condition names that have
an errmodel mapping. NULL means \code{errmodel} applies to all.}

\item{bessel}{Bessel correction factor (default 1, matching the
\code{use.bessel = FALSE} branch of \code{normL2}).}

\item{deriv, deriv2}{Logical. Whether to return gradient/Hessian.}
}
\value{
An \link{objlist} for the single condition's contribution.
}
\description{
Computes the negative-log-likelihood residual contribution of a single
condition (with optional error model). Lifted out of \link{normL2} so the ECM
per-subject node-loop in \link{nlmeFit} / \code{emObjfn(method="quadrature")} can reuse
the exact contract without re-routing through normL2's multi-condition
machinery and parameter-name binding for every quadrature node.

Behavior matches the inner closure that previously lived inside \code{normL2}
verbatim; \code{normL2} now calls this helper.
}
