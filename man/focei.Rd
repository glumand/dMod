% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foceiClass.R
\name{focei}
\alias{focei}
\title{FOCEI bilevel estimator}
\usage{
focei(
  joint,
  omegaSpec,
  model = NULL,
  data = NULL,
  innerControl = list(),
  correction = c("none", "lagged", "eager"),
  correctionControl = list(tau = 0.1, M = 5L),
  cores = 1L
)
}
\arguments{
\item{joint}{An \code{objfn} typically built as
\code{normL2(data, g*x*p, errmodel = err) + constraintL2(mu = 0, Omega = om)}.
It must accept the full parameter set including all subject-level eta
parameters listed in \code{omegaSpec$subject_etas}. Subjects must be encoded as
dMod conditions (one condition per subject), typically created via
\link{branch} with one row per subject in the substitution table.}

\item{omegaSpec}{An \code{omegaSpec} object with subject expansion (see \link{omega}).
Determines the subjects, the K random-effect names per subject, and the
Cholesky parametrisation of \eqn{\Omega}.}

\item{model}{Optional \code{prdfn} (typically \code{g * x * p}) supporting
\code{deriv2 = TRUE}. Required when \code{correction != "none"} so the analytical
\verb{\\partial \\log|H_i|/\\partial \\theta} can be assembled from
\verb{\\partial^2 g / \\partial \\eta \\partial \\theta}. Pass the same prediction
function that was used to build \code{joint}.}

\item{data}{Optional \link{datalist} supplying per-observation \code{sigma} for the
correction's residual weighting. Required when \code{correction != "none"}.
Stage 2 restriction: \code{sigma} may depend on \code{theta} but not on \code{eta}
(no error-model interaction with random effects yet).}

\item{innerControl}{Named list of inner-trust options. Recognised entries:
\code{rtol} (relative termination tolerance, default \code{1e-6}), \code{maxit} (maximum
inner iterations, default \code{50}), \code{rinit}/\code{rmax} (initial / maximum
trust-region radius, default \code{1} / \code{10}).}

\item{correction}{One of \code{"none"} (default, Stage 1 envelope-only gradient,
no \verb{\\log|H_i|} correction), \code{"lagged"} (Stage 2 analytical correction with
BDF-style cache + refresh triggers), or \code{"eager"} (Stage 2 analytical
correction recomputed every outer iteration, mostly for testing parity).}

\item{correctionControl}{Named list of cache-refresh options for
\code{correction = "lagged"}. Recognised entries: \code{tau} (relative
anchor-distance threshold, default \code{0.1}) and \code{M} (periodic refresh
interval, default \code{5L}). A refresh additionally fires on cold start, on
any rejected outer step (when the \link{trust} \code{on_step} hook is wired up),
and when \code{last_rho < 0.25}.}

\item{cores}{Integer. Number of cores for the per-subject inner solves.
Default \code{1}. On Windows, a \code{parLapply} cluster is created on the fly; on
Unix-likes, \code{mclapply} is used.}
}
\value{
An object of class \code{c("focei", "objfn", "fn")} callable as
\code{obj(pars, fixed = NULL, deriv = TRUE, env = NULL)}. Returns an \code{objlist}
whose \code{attr(., "focei_diag")} carries per-subject diagnostics
(\eqn{\eta_i^*}, eigenvalues of \eqn{H_i}, \eqn{\log|H_i|}, count of
floored eigenvalues, inner iteration count).
}
\description{
Wraps a user-built joint objective into a FOCEI (First-Order Conditional
Estimation with Interaction) marginal-likelihood approximation. The returned
objfn lives over the population-level parameters
\eqn{(\theta, \Omega, \Sigma)}; the subject-level random effects \eqn{\eta_i}
are integrated out by Laplace approximation at each evaluation.
}
\details{
The estimator implements the standard FOCEI bilevel structure. For each
outer evaluation:
\enumerate{
\item For each subject \eqn{i}, an inner \link{trust}-region optimisation
finds \eqn{\eta_i^* = \mathrm{argmin}_\eta J(\theta, \eta, \Omega, \Sigma; \mathrm{data}_i)}
starting from a warmstart cache.
\item The Hessian \eqn{H_i = \partial^2 J / \partial \eta^2} is read off
the converged inner solver and spectrally regularised: an eigenvalue
floor \eqn{\epsilon = 10^{-10}\,\mathrm{tr}(H_i)/K} is applied so
\eqn{\log|H_i|} and \eqn{H_i^{-1}} stay numerically well-defined.
\item The marginal objective is
\deqn{\mathrm{OFV} = J(\theta, \eta^*, \Omega, \Sigma) + \sum_i \log|H_i|.}
\item The outer gradient is taken from \code{joint} evaluated at
\eqn{(\theta, \eta^*)}; by the envelope theorem,
\eqn{\partial J/\partial \eta = 0} at \eqn{\eta^*}, so only direct
partials wrt \eqn{(\theta, \Omega, \Sigma)} contribute.
\item The outer Hessian is the Gauss-Newton Schur complement of \code{joint}'s
full Hessian after eliminating the eta block per subject:
\eqn{H_{\theta\theta} - \sum_i H_{\theta\eta_i}^{(i)} H_{\eta\eta}^{(i)\,-1} H_{\eta_i\theta}^{(i)}}.
}

Stage 2 correction: setting \code{correction = "lagged"} (or \code{"eager"}) restores
the missing \verb{\\partial \\log|H_i|/\\partial \\theta} term in the outer gradient.
The correction is built analytically from second-order prediction
sensitivities (\code{deriv2 = TRUE} on \code{model}) and a Gauss-Newton form of
\eqn{H_i = G_i^\top \Sigma^{-1} G_i + \Omega^{-1}}. Lagging caches the
correction across outer iterations and refreshes only on rejected steps,
large parameter excursions, or every \code{M} iterations; the cache is also
invalidated on any rejected outer step provided the caller wires the
\link{trust} \code{on_step} hook (see Examples).
}
\examples{
\dontrun{
# ... build model, data, joint, omega spec ...
focei_obj <- focei(joint, om, model = g*x*p, data = dlist,
                   correction = "lagged")
fit <- trust(focei_obj, init,
             rinit = 1, rmax = 10, iterlim = 200,
             on_step = attr(focei_obj, "on_step"))
}
}
\seealso{
\link{omega}, \link{constraintL2}, \link{trust}
}
